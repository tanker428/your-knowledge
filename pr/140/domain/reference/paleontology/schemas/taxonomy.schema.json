{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "your-knowledge/reference/taxonomy.schema.json",
  "type": "object",
  "required": ["schemaVersion", "meta", "nodes", "relations"],
  "properties": {
    "schemaVersion": { "type": "string" },
    "meta": {
      "type": "object",
      "required": ["id", "axis", "source"],
      "properties": {
        "id": { "type": "string" },
        "axis": { "enum": ["taxonomy", "classification"] },
        "source": { "type": "object", "required": ["status"] }
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "label", "axis", "status", "internalOnly", "ui"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "label": { "type": "string", "minLength": 1 },
          "axis": { "enum": ["taxonomy", "classification"] },
          "parentId": { "type": ["string", "null"] },
          "parentIds": { "type": "array", "items": { "type": "string" } },
          "status": { "enum": ["draft", "verified", "deprecated"] },
          "internalOnly": { "type": "boolean" },
          "ui": { "type": "object", "required": ["visible"] },
          "externalIds": { "type": "object" }
        }
      }
    },
    "relations": { "type": "array" }
  }
}
